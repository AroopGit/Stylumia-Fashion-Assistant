# NGINX Load Balancer Configuration (Showcase Only)
# This file is a template for load balancing multiple backend servers.
# All lines are commented out for demonstration purposes.

# http {
#     upstream stylumia_backend {
#         server backend1.example.com:8000;
#         server backend2.example.com:8000;
#         # Add more backend servers as needed
#     }
#
#     server {
#         listen 80;
#         server_name example.com;
#
#         location / {
#             proxy_pass http://stylumia_backend;
#             proxy_set_header Host $host;
#             proxy_set_header X-Real-IP $remote_addr;
#             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#             proxy_set_header X-Forwarded-Proto $scheme;
#         }
#     }
# } 